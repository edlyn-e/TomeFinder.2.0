import React from "react";
import styles from "./Footer.module.scss";

const Footer = () => {
    return (
        <div className={styles.Footer}>
            <h4>Find your next read with the power of Google Books API</h4>
            <h5>built by edlyn-e</h5>
        </div>
    );
};

export default Footer;
